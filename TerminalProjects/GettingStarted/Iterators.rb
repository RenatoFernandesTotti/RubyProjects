limiter = 0

while limiter < 9 #do is not requires but is easier to read
  limiter += 1
end

puts "#{limiter}"

for i in 1..5 #do is not requires but is easier to read
  #note 1..N is inclusive
  puts "#{i}"
end
